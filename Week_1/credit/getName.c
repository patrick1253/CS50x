// a helper function to be added to credit.c to get a credit card fromm a user, validate that it is a 
//plausible number, and which credit card company is the issuer.

#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    getNum(char* number, int length);
}